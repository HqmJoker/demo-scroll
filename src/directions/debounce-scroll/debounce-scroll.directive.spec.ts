import { DebounceScrollDirective } from './debounce-scroll.directive';

describe('DebounceScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new DebounceScrollDirective();
    expect(directive).toBeTruthy();
  });
});
